{
  "version": "0.0.1",
  "configurations": [
    {
      "name": "vite",
      "runtimeExecutable": "sh",
      "runtimeArgs": [
        "-c",
        "if [ -s \"$HOME/.nvm/nvm.sh\" ]; then . \"$HOME/.nvm/nvm.sh\" >/dev/null 2>&1; nvm use --silent 22 >/dev/null 2>&1 || nvm use --silent 20 >/dev/null 2>&1 || true; fi; exec npm run dev"
      ],
      "port": 5173
    }
  ]
}
