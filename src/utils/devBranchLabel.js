/**
 * In development, set in vite.config.js from `git rev-parse --abbrev-ref HEAD`
 * (falling back to VITE_BRANCH_NAME or "local"). Empty string in production builds.
 */
export const DEV_BRANCH_LABEL = __RSM_DEV_BRANCH_LABEL__
